
    <div class="cat-tags">
        <div class="container">
            <ul class="catagory">
                <li><a href="{{ blog.url }}" class="{% if current_tags != tag %}active{% endif %}">All Categories</a></li>
                <!-- ==== start loop ==== -->              
                    {% for tag in article.tags %}                       
                      <li>
                      <a {% if current_tags contains tag %}class="active" {% endif %} href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag }}</a>
                        </li>                            
              {% endfor %}
                <!-- ======= end loop ====== -->
            </ul>
        </div>
    </div>
    <section class="blogs-main padding-main">
        <div class="container">
            <div class="blogs-flex">
                <!-- ==== start loop ===== -->
                {% for article in blogs['blog'].articles reversed %}
                      {% render 'custom-article-card', article: article %}
                {% endfor %}
                </div>
                <!-- ======== end loop ==== -->
            </div>
        </div>
    </section>
{% schema %}
  {
    "name": "All Blog Posts",
    "settings": [],
    "presets": [
      {
        "name": "All Blog Posts"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}